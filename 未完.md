# 完善清单

- [x] 页头增加主题切换按钮，可在系统、浅色、深色之间循环切换，并保留设置页的三档选择。
- [x] 已确认当前位于 `main` 分支，原有 `debug` 分支已通过合并提交进入 `main`，且当时与 `origin/main` 一致。
- [x] 将前 500 常用字按字频分成 10 组，每组 50 字；每组进入练习后都可单独乱序。
- [x] 改进文章生成器，去除跨文章的整句复用，重新生成 200 篇离线文章，并增加“不得跨文章重复句子”的自动测试。

## 验证

- `npm run data:generate`
- `npm test`
- `npm run lint`
- `npx tsc --noEmit`
